import { ProductsView } from "@/features/products/_components/products-view";

/** /products — product management: list, create, edit, add stock, deactivate. */
const ProductsPage = () => <ProductsView />;

export default ProductsPage;
