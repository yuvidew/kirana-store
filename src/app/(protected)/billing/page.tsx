/** /billing — placeholder until Phase 3 builds bill generation. */
const BillingPage = () => {
  return (
    <div className="p-6">
      <h1 className="text-lg font-medium">Billing</h1>
      <p className="text-sm text-muted-foreground">
        Built in Phase 3 — this placeholder confirms the authenticated route is reachable.
      </p>
    </div>
  );
};

export default BillingPage;
