/** /dashboard — placeholder until Phase 4 builds the real dashboard. */
const DashboardPage = () => {
  return (
    <div className="p-6">
      <h1 className="text-lg font-medium">Dashboard</h1>
      <p className="text-sm text-muted-foreground">
        Built in Phase 4 — this placeholder confirms the authenticated route is reachable.
      </p>
    </div>
  );
};

export default DashboardPage;
